import { PrixTotalPanierPipe } from './prix-total-panier.pipe';

describe('PrixTotalPanierPipe', () => {
  it('create an instance', () => {
    // @ts-ignore
    const pipe = new PrixTotalPanierPipe();
    expect(pipe).toBeTruthy();
  });
});
